# External data

Files placed here are installed with the package and accessible via
`system.file("extdata", "<filename>", package = "ettbc")`.

This directory is currently empty. The package's synthetic example datasets
(`cohort`, `screening_mammograms`, `diagnostic_mammograms`) are shipped as
lazy-loaded data objects in `data/`, not as files here.
