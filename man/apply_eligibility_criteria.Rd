% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/apply_eligibility_criteria.R
\name{apply_eligibility_criteria}
\alias{apply_eligibility_criteria}
\title{Apply Cohort Eligibility Criteria}
\usage{
apply_eligibility_criteria(
  demographics,
  enrollment,
  mammograms,
  id_col = "id",
  agein_month_col = "agein_month",
  death_month_col = "death_month",
  orec_col = "orec",
  enrollment_month_col = "month",
  buyin_col = "buyin",
  hmo_col = "hmo",
  mammo_month_col = "month",
  eligible_buyin = c("3", "C"),
  eligible_hmo = "0",
  mammo_window_months = 12L,
  enrollment_window_months = 12L
)
}
\arguments{
\item{demographics}{A data frame with one row per participant, containing
the participant ID, the month each participant turns the entry age
(\code{agein_month_col}), month of death (\code{death_month_col}, \code{NA} if alive),
and original reason for entitlement (\code{orec_col}).}

\item{enrollment}{A data frame with one row per participant-month,
containing the participant ID, calendar month (\code{enrollment_month_col}),
fee-for-service buy-in code (\code{buyin_col}), and HMO enrollment indicator
(\code{hmo_col}). A participant-month absent from \code{enrollment} is treated as
not enrolled.}

\item{mammograms}{A data frame of screening mammogram events, containing
the participant ID and event month (\code{mammo_month_col}).}

\item{id_col}{Name of the participant ID column in \code{demographics},
\code{enrollment}, and \code{mammograms}. Default: \code{"id"}.}

\item{agein_month_col}{Name of the age-threshold month column in
\code{demographics}. Default: \code{"agein_month"}.}

\item{death_month_col}{Name of the death-month column in \code{demographics}
(\code{NA} if alive). Default: \code{"death_month"}.}

\item{orec_col}{Name of the original-reason-for-entitlement column in
\code{demographics}. Default: \code{"orec"}.}

\item{enrollment_month_col}{Name of the calendar-month column in
\code{enrollment}. Default: \code{"month"}.}

\item{buyin_col}{Name of the fee-for-service buy-in code column in
\code{enrollment}. Default: \code{"buyin"}.}

\item{hmo_col}{Name of the HMO enrollment indicator column in
\code{enrollment}. Default: \code{"hmo"}.}

\item{mammo_month_col}{Name of the event-month column in \code{mammograms}.
Default: \code{"month"}.}

\item{eligible_buyin}{Buy-in codes that count as fee-for-service Part A+B
enrollment. Default: \code{c("3", "C")}.}

\item{eligible_hmo}{HMO indicator value(s) that count as \emph{not} enrolled in
an HMO. Default: \code{"0"}.}

\item{mammo_window_months}{Width, in months, of the window starting at
\code{agein_month_col} in which a screening mammogram qualifies as the entry
event. Default: \code{12L} (approximating the 365-day window in the original
study).}

\item{enrollment_window_months}{Number of consecutive months of
enrollment, ending at and including the entry month, required for
eligibility. Default: \code{12L}.}
}
\value{
A data frame with one row per eligible participant:
\itemize{
\item \code{id}: Participant ID (named as \code{id_col}).
\item \code{agein_month}: The age-threshold month from \code{demographics} (named as
\code{agein_month_col}).
\item \code{start_month}: The derived study entry month (the qualifying screening
mammogram month).
}

Join this onto the participant's other cohort columns (e.g. \code{end_month},
\code{death_month}, \code{bc_death}, \code{bc_month}) to build the \code{data} input expected
by \code{\link[=clone_censor]{clone_censor()}}.
}
\description{
Selects the participants eligible for entry into the target trial and
determines each one's study entry month, following the eligibility
criteria of García-Albéniz et al. (2020) (SAS \code{c01_eligibility.sas}):
alive at the age threshold, a qualifying screening mammogram near that
age, twelve months of continuous fee-for-service Medicare enrollment
ending at entry, and Medicare entitlement by age (not disability or
end-stage renal disease).
}
\details{
Four criteria are applied, in order:
\enumerate{
\item \strong{Alive at the age threshold.} A participant must be alive when they
reach \code{agein_month_col} (a \code{death_month_col} at or after
\code{agein_month_col}, or missing, still counts as alive at that month).
\item \strong{A qualifying screening mammogram.} The earliest screening mammogram
in the \code{mammo_window_months}-month window starting at
\code{agein_month_col} becomes the participant's study entry month
(\code{start_month}). Participants without one are excluded.
\item \strong{Continuous enrollment.} Every one of the \code{enrollment_window_months}
months ending at the entry month must have a fee-for-service enrollment
record (\code{buyin_col} value in \code{eligible_buyin}) with no HMO enrollment
(\code{hmo_col} value in \code{eligible_hmo}). A participant-month absent from
\code{enrollment}, or one outside these codes, disqualifies the participant.
\item \strong{Original reason for entitlement.} \code{orec_col} must equal \code{0}
(entitled by age, not disability or end-stage renal disease); a
missing \code{orec_col} disqualifies the participant.
}

The fee-for-service buy-in codes (\code{"3"}, \code{"C"}) and the not-in-an-HMO code
(\code{"0"}) are the Medicare \code{BUYIN}/\code{HMOIND} values used directly by
\code{c01_eligibility.sas}.
}
\examples{
# Participant 1 is eligible; participant 2 dies before the age threshold;
# participant 3 has no qualifying mammogram in the entry window.
demographics <- data.frame(
  id = 1:3,
  agein_month = c(12, 12, 12),
  death_month = c(NA, 5, NA),
  orec = c(0, 0, 0)
)
enrollment <- data.frame(
  id = rep(1, 12), month = 1:12, buyin = "3", hmo = "0"
)
mammograms <- data.frame(id = c(1, 3), month = c(12, 30))
apply_eligibility_criteria(demographics, enrollment, mammograms)
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=clone_censor]{clone_censor()}} for the step this cohort feeds into, and
\code{\link[=comorbidity_score]{comorbidity_score()}} for the comorbidity-adjustment side of cohort
construction.
}
