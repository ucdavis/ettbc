% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/clone_censor.R
\name{clone_censor}
\alias{clone_censor}
\title{Clone and Censor Participants for Target Trial Emulation}
\usage{
clone_censor(
  data,
  scr_mammo,
  dx_mammo,
  id_col = "id",
  start_col = "start_month",
  end_col = "end_month",
  death_col = "death_month",
  bc_death_col = "bc_death",
  bc_month_col = "bc_month",
  mammo_month_col = "month",
  stop_grace_months = 9L,
  continue_grace_months = 13L,
  total_months = 108L
)
}
\arguments{
\item{data}{A data frame with one row per participant, containing columns
for participant ID, study entry month, last follow-up month, death month,
breast cancer death indicator, and breast cancer diagnosis month.}

\item{scr_mammo}{A data frame with one row per screening mammogram event,
containing columns for participant ID and event month.}

\item{dx_mammo}{A data frame with one row per diagnostic mammogram event,
containing columns for participant ID and event month.}

\item{id_col}{Name of the participant ID column in \code{data}, \code{scr_mammo}, and
\code{dx_mammo}. Default: \code{"id"}.}

\item{start_col}{Name of the study entry month column in \code{data}.
Default: \code{"start_month"}.}

\item{end_col}{Name of the last follow-up month column in \code{data}
(administrative censoring, e.g., end of study or loss to follow-up).
Default: \code{"end_month"}.}

\item{death_col}{Name of the death month column in \code{data}
(\code{NA} if alive at end of follow-up). Default: \code{"death_month"}.}

\item{bc_death_col}{Name of the breast cancer death indicator column in
\code{data} (0/1). Default: \code{"bc_death"}.}

\item{bc_month_col}{Name of the breast cancer diagnosis month column in
\code{data} (\code{NA} if no diagnosis). Default: \code{"bc_month"}.}

\item{mammo_month_col}{Name of the mammogram month column in \code{scr_mammo}
and \code{dx_mammo}. Default: \code{"month"}.}

\item{stop_grace_months}{Number of months after study entry during which a
screening mammogram does not trigger censoring in the STOPBASE arm.
Default: \code{9}.}

\item{continue_grace_months}{Length of the compliance window (in months) in
the CONTINUE arm. A participant remains on-protocol for this many months
following any mammogram (screening or diagnostic). Default: \code{13}.}

\item{total_months}{Total number of months in the study period.
Default: \code{108} (corresponding to January 2000 through December 2008).}
}
\value{
A data frame with two rows per participant (one per arm), containing
all columns from \code{data} plus:
\describe{
\item{\code{arm}}{Trial arm: \code{"STOPBASE"} or \code{"CONTINUE"}}
\item{\code{end_month}}{Last observed month (minimum of death, administrative
censoring, and protocol censoring). Overwrites the input \code{end_col}
column.}
\item{\code{censor_month}}{Month of protocol deviation censoring,
or \code{NA} if the participant was not censored for protocol
non-adherence.}
\item{\code{fup}}{Follow-up time in months (\code{end_month - start_month + 1}).}
\item{\code{died}}{Overall mortality indicator (0/1): 1 if death was the
reason for ending follow-up.}
\item{\code{bc_died}}{Breast cancer mortality indicator (0/1).}
}
}
\description{
Creates two clones of each participant — one assigned to each of two
hypothetical trial arms — and applies the corresponding censoring rules.
This implements the "clone-censor" step of the target trial emulation
methodology described in García-Albéniz et al. (2020).
}
\details{
Each participant is cloned into two arms:
\itemize{
\item \strong{STOPBASE}: The clone is censored the first time the participant receives
a screening mammogram after the initial grace period
(\code{stop_grace_months}). Within the grace period, and after a breast cancer
diagnosis, screening mammograms do not trigger censoring.
\item \strong{CONTINUE}: The clone is censored the first time the participant goes
longer than \code{continue_grace_months} months without any mammogram (screening
or diagnostic). Breast cancer diagnosis prevents censoring.
}

Months are numbered as consecutive integers starting from 1 (representing
the first month of the study period), matching the convention used in the
original SAS implementation.
}
\examples{
cloned <- clone_censor(
  cohort,
  screening_mammograms,
  diagnostic_mammograms
)
head(cloned)
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381–389. \doi{10.7326/M18-1199}

Hernán MA, Robins JM. Using Big Data to Emulate a Target Trial When a
Randomized Trial Is Not Available. \emph{Am J Epidemiol.}
2016;183(8):758–764. \doi{10.1093/aje/kwv254}
}
\seealso{
\code{\link[=expand_to_long]{expand_to_long()}} for expanding the cloned dataset to one row per
participant-arm-month.
}
