% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/negative_control_analysis.R
\name{negative_control_analysis}
\alias{negative_control_analysis}
\title{Negative-Control Outcome (Falsification) Analysis}
\usage{
negative_control_analysis(
  long_data,
  covariate_cols = NULL,
  weight_col = "wp99",
  outcome_col = "nc_dead_t1",
  arm_col = "arm",
  month_col = "month2",
  id_col = "id",
  cluster_id_col = id_col,
  max_month = 95L,
  rcs_knots = c(6, 48, 72),
  null_value = 1
)
}
\arguments{
\item{long_data}{A data frame in long format (one row per
participant-arm-month), as produced by \code{\link[=expand_to_long]{expand_to_long()}}.}

\item{covariate_cols}{Character vector of column names to include as
additional baseline adjustment terms. Set to \code{NULL} for no adjustment.
Default: \code{NULL}.}

\item{weight_col}{Name of the column containing IPW weights. Set to \code{NULL}
for unweighted estimation. Default: \code{"wp99"}.}

\item{outcome_col}{Name of the negative-control outcome column, as produced
by \code{\link[=expand_to_long]{expand_to_long()}}. Default: \code{"nc_dead_t1"}.}

\item{arm_col}{Name of the trial arm column. Default: \code{"arm"}.}

\item{month_col}{Name of the 0-indexed month-from-entry column.
Default: \code{"month2"}.}

\item{id_col}{Name of the participant identifier column. Default: \code{"id"}.}

\item{cluster_id_col}{Name of the column to use for clustering standard
errors. When non-\code{NULL} and the \code{sandwich} package is available,
cluster-robust confidence intervals are returned. Defaults to \code{id_col}.}

\item{max_month}{Maximum month included in the model. Rows beyond this
month are excluded. Default: \code{95L}.}

\item{rcs_knots}{Numeric vector with at least 3 elements specifying the
knots for the restricted cubic spline on time: the first element is the
left boundary knot, the last element is the right boundary knot, and any
middle elements are interior knots. Must have at least one interior knot.
Default: \code{c(6, 48, 72)} (one interior knot at month 48).}

\item{null_value}{Odds ratio implying no arm effect. Default: \code{1}.}
}
\value{
A named list:
\itemize{
\item \code{or}: Odds ratio for the STOPBASE arm on the negative-control outcome.
\item \code{or_ci}: 95\% confidence interval for \code{or}.
\item \code{null_consistent}: \code{TRUE} when \code{or_ci} covers \code{null_value}, i.e., the
falsification test passes (no detectable effect on the negative control).
\item \code{model}: The fitted \link[stats:glm]{stats::glm} object.
}
}
\description{
Runs the IP-weighted outcome model on a negative-control outcome -- a cause
of death that continued screening mammography cannot plausibly affect -- and
reports whether the estimated arm effect is consistent with the null. A
clearly non-null association on the negative-control outcome would point to
residual confounding or selection bias rather than a true screening effect.
}
\details{
This ports the negative-control falsification check of García-Albéniz et al.,
which used death from cancer of the corpus uteri as the negative-control
outcome (supplementary analysis). The function reuses \code{\link[=fit_outcome_hr]{fit_outcome_hr()}} on
the negative-control outcome column produced by \code{\link[=expand_to_long]{expand_to_long()}} when
called with its \code{nc_died_col} argument (\code{nc_dead_t1} by default). The
returned odds ratio approximates the hazard ratio for the STOPBASE arm
relative to CONTINUE on the negative-control outcome; absent bias it should
sit near \code{null_value}, and \code{null_consistent} records whether the confidence
interval covers it.
}
\examples{
sim <- simulate_screening_cohort(n = 800, seed = 4, negative_control = TRUE)
cloned <- clone_censor(
  sim$cohort, sim$screening_mammograms, sim$diagnostic_mammograms
)
long <- expand_to_long(cloned, nc_died_col = "nc_death")
long <- augment_long_covariates(
  long, sim$screening_mammograms, sim$diagnostic_mammograms
)
fit <- fit_screening_propensity(long)
weighted <- compute_ipw_weights(fit$data, pred_prob_col = "p_scrmammo")
nc <- negative_control_analysis(weighted)
nc$or
nc$null_consistent
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=fit_outcome_hr]{fit_outcome_hr()}} for the underlying model and \code{\link[=expand_to_long]{expand_to_long()}}
(argument \code{nc_died_col}) for building the negative-control outcome.
}
