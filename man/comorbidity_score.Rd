% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/comorbidity_score.R
\name{comorbidity_score}
\alias{comorbidity_score}
\title{Weighted Comorbidity Score}
\usage{
comorbidity_score(data, weights = gagne_weights(), na_rm = FALSE)
}
\arguments{
\item{data}{A data frame with one row per person and a 0/1 (or logical)
indicator column for each condition named in \code{weights}.}

\item{weights}{A named numeric vector mapping condition column names to
their weights. Defaults to \code{\link[=gagne_weights]{gagne_weights()}} (the Gagne combined
comorbidity score); \code{data} must then contain all 20 Gagne condition
columns.}

\item{na_rm}{Logical; if \code{TRUE}, missing indicator values are treated as 0
(condition absent). If \code{FALSE} (the default), any \code{NA} in a scored column
is an error.}
}
\value{
A numeric vector with one weighted score per row of \code{data}.
}
\description{
Computes a weighted comorbidity score for each person from a set of binary
condition indicators and a named vector of per-condition weights. This is the
general scoring engine behind the Gagne combined comorbidity score (see
\code{\link[=gagne_weights]{gagne_weights()}}); supplying a different \code{weights} vector computes any other
additive comorbidity index from the same condition flags.
}
\details{
The score for a person is the sum, over the conditions named in \code{weights}, of
the weight times the person's 0/1 indicator for that condition. The
comorbidity-mapping step (turning ICD diagnosis codes into the condition
indicators) is out of scope here; map codes to flags first --- for example
with the \href{https://CRAN.R-project.org/package=comorbidity}{comorbidity}
package --- then pass the resulting indicator columns to this function.
}
\examples{
people <- data.frame(
  chf = c(0, 1, 0),
  dementia = c(0, 0, 1),
  metastatic_cancer = c(0, 0, 1)
)
w <- c(chf = 2, dementia = 2, metastatic_cancer = 5)
comorbidity_score(people, w)
}
\references{
Gagne JJ, Glynn RJ, Avorn J, Levin R, Schneeweiss S. A combined comorbidity
score predicted mortality in elderly patients better than existing scores.
\emph{J Clin Epidemiol.} 2011;64(7):749-759. \doi{10.1016/j.jclinepi.2010.10.004}
}
\seealso{
\code{\link[=gagne_weights]{gagne_weights()}} for the García-Albéniz / Gagne weight set.
}
