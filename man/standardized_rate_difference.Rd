% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/standardized_rate_difference.R
\name{standardized_rate_difference}
\alias{standardized_rate_difference}
\title{Direct-Standardized Rate Difference Between Arms}
\usage{
standardized_rate_difference(
  data,
  outcome_col,
  strata_cols,
  arm_col = "arm",
  mult = 1,
  n_boot = 500L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{data}{A data frame with one row per person (or person-arm), containing
the outcome, arm, and stratifying columns.}

\item{outcome_col}{Name of the binary (0/1) outcome column (e.g., receipt of
surgery within 12 months).}

\item{strata_cols}{Character vector of stratifying column names (e.g., age
group and comorbidity score) defining the standardization strata.}

\item{arm_col}{Name of the trial arm column (\code{"CONTINUE"} / \code{"STOPBASE"}).
Default: \code{"arm"}.}

\item{mult}{Rate multiplier; the rates and their difference are scaled by
this. Default: \code{1} (proportions). Use \code{100} to match the SAS output.}

\item{n_boot}{Number of bootstrap resamples. Default: \code{500L}.}

\item{conf_level}{Width of the percentile confidence interval.
Default: \code{0.95}.}

\item{seed}{Optional integer seed; the caller's RNG state is restored on
exit. Default: \code{NULL}.}
}
\value{
A named list with \code{rate_continue} and \code{rate_stopbase} (the
standardized arm rates), \code{diff} (their difference), \code{conf_low},
\code{conf_high}, \code{n_boot}, and \code{conf_level}. All rates are scaled by \code{mult}.
}
\description{
Computes the difference in a binary outcome rate between the CONTINUE and
STOPBASE arms, \strong{directly standardized} to the pooled sample over a set of
stratifying variables, with a nonparametric bootstrap percentile confidence
interval. This ports the treatment-pattern secondary analyses of
García-Albéniz et al. (SAS \code{cann26}/\code{cann27}/\code{cann28}: surgery,
chemotherapy, and radiotherapy receipt among screen-detected cancers),
which used \verb{PROC STDRATE} (\code{method = direct}, \code{effect = diff}) standardized
by age and comorbidity.
}
\details{
For each arm the stratum-specific outcome rate is computed, then averaged
using the pooled-sample stratum distribution as the reference (direct
standardization). The standardized rate difference is
\code{rate(CONTINUE) - rate(STOPBASE)}. Standardization is restricted to strata
present in \strong{both} arms (common support); the reference weights are
rescaled to sum to one over those strata. Multiply by \code{mult} to report a
rate per
\code{mult} people (the SAS code used \code{mult = 100}).

The confidence interval is the central \code{conf_level} percentile range of the
standardized rate difference across \code{n_boot} bootstrap resamples of the rows
(reference weights are recomputed on each resample). The caller's RNG state
is restored on exit.
}
\examples{
set.seed(1)
n <- 400
dat <- data.frame(
  arm = rep(c("CONTINUE", "STOPBASE"), each = n / 2),
  age_group = sample(c("70-74", "75-84"), n, replace = TRUE),
  surgery = rbinom(n, 1, 0.5)
)
standardized_rate_difference(
  dat, "surgery", strata_cols = "age_group", n_boot = 200, seed = 1
)
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
