% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/bootstrap_ci.R
\name{bootstrap_ci}
\alias{bootstrap_ci}
\title{Bootstrap Confidence Intervals for Survival Difference}
\usage{
bootstrap_ci(
  long_data,
  pred_prob_col,
  covariate_cols = NULL,
  id_col = "id",
  outcome_col = "dead_t1",
  arm_col = "arm",
  month_col = "month2",
  max_month = 95L,
  rcs_knots = c(6, 48, 72),
  n_boot = 500L,
  seed = NULL,
  fail_threshold = 0.1
)
}
\arguments{
\item{long_data}{A data frame in long format (one row per
participant-arm-month), as produced by \code{\link[=expand_to_long]{expand_to_long()}} and augmented
with the columns required by \code{\link[=compute_ipw_weights]{compute_ipw_weights()}}.}

\item{pred_prob_col}{Name of the column containing the model-predicted
probability of a screening mammogram, passed to \code{\link[=compute_ipw_weights]{compute_ipw_weights()}}.}

\item{covariate_cols}{Character vector of covariate column names for
\code{\link[=predict_survival_ipw]{predict_survival_ipw()}}. Set to \code{NULL} for unadjusted estimation.
Default: \code{NULL}.}

\item{id_col}{Name of the participant identifier column. Default: \code{"id"}.}

\item{outcome_col}{Name of the binary outcome column. Default: \code{"dead_t1"}.}

\item{arm_col}{Name of the trial arm column. Default: \code{"arm"}.}

\item{month_col}{Name of the 0-indexed month-from-entry column.
Default: \code{"month2"}.}

\item{max_month}{Maximum month for survival prediction. Default: \code{95L}.}

\item{rcs_knots}{Numeric vector of length 3 specifying the restricted cubic
spline knots. Default: \code{c(6, 48, 72)}.}

\item{n_boot}{Number of bootstrap iterations. Default: \code{500L}.}

\item{seed}{Integer seed for reproducibility. \code{NULL} means no seed is set.
Default: \code{NULL}.}

\item{fail_threshold}{Maximum proportion of bootstrap iterations that may
fail before a warning is issued. Default: \code{0.1} (warn if more than 10\%
of iterations fail).}
}
\value{
A data frame with one row per month (0 through \code{max_month}),
containing:
\itemize{
\item \code{month}: Month index (0-indexed from trial entry).
\item \code{diff}: Point estimate of \code{s_continue - s_stopbase}.
\item \code{diff_lo}: 2.5th percentile bootstrap estimate of the survival
difference.
\item \code{diff_hi}: 97.5th percentile bootstrap estimate of the survival
difference.
\item \code{s_continue}: Point estimate of survival in the CONTINUE arm.
\item \code{s_stopbase}: Point estimate of survival in the STOPBASE arm.
\item \code{s_continue_lo}: 2.5th percentile bootstrap estimate for CONTINUE
survival.
\item \code{s_continue_hi}: 97.5th percentile bootstrap estimate for CONTINUE
survival.
\item \code{s_stopbase_lo}: 2.5th percentile bootstrap estimate for STOPBASE
survival.
\item \code{s_stopbase_hi}: 97.5th percentile bootstrap estimate for STOPBASE
survival.
}
}
\description{
Uses the nonparametric bootstrap to compute 95\% percentile confidence
intervals for the difference in marginal survival between the CONTINUE and
STOPBASE arms, as estimated by \code{\link[=predict_survival_ipw]{predict_survival_ipw()}}.
}
\details{
At each bootstrap iteration, participant IDs are sampled with replacement.
All long-format rows belonging to a sampled ID are included in the bootstrap
dataset, with duplicate draws receiving distinct synthetic identifiers. IPW
weights are recomputed on each bootstrap sample before estimating survival
curves.

The point estimate uses the original (unbootstrapped) data. Confidence
intervals are taken as the 2.5th and 97.5th percentiles of the bootstrap
distribution.
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381–389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=predict_survival_ipw]{predict_survival_ipw()}}, \code{\link[=compute_ipw_weights]{compute_ipw_weights()}}
}
