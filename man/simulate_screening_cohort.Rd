% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/simulate_screening_cohort.R
\name{simulate_screening_cohort}
\alias{simulate_screening_cohort}
\title{Simulate a Synthetic Breast Cancer Screening Cohort}
\usage{
simulate_screening_cohort(n = 100L, max_month = 108L, seed = NULL)
}
\arguments{
\item{n}{Number of participants to simulate. Default: \code{100L}.}

\item{max_month}{Total number of months in the study period. Default:
\code{108L} (January 2000 through December 2008).}

\item{seed}{Optional integer seed for reproducibility. \code{NULL} (the default)
leaves the random-number stream untouched.}
}
\value{
A named list of three linked data frames:
\itemize{
\item \code{cohort}: One row per participant, as documented in \link{cohort}.
\item \code{screening_mammograms}: One row per screening-mammogram event, as
documented in \link{screening_mammograms}.
\item \code{diagnostic_mammograms}: One row per diagnostic-mammogram event, as
documented in \link{diagnostic_mammograms}.
}
}
\description{
Generates a synthetic cohort of arbitrary size with the structure required
by the \code{{ettbc}} pipeline: a participant table plus linked screening- and
diagnostic-mammogram event tables. The values are entirely simulated and do
not represent real patients; the function exists so analyses, examples, and
the package vignettes can be demonstrated end to end without Medicare claims.
}
\details{
The three returned data frames are produced, in order, by the package's
internal generators (\code{simulate_cohort()},
\code{simulate_screening_mammograms()}, and \code{simulate_diagnostic_mammograms()}),
sharing a single random-number stream. Calling
\code{simulate_screening_cohort(100, 108, seed = 2020)} therefore reproduces the
shipped \link{cohort}, \link{screening_mammograms}, and \link{diagnostic_mammograms}
example datasets exactly.

Roughly 80\% of participants are annual "adherers"; about 15\% die during
follow-up and about 2\% die from breast cancer. Months are numbered
consecutively from 1, matching the convention used throughout the package.
Each participant has a pre-entry screening mammogram so the CONTINUE arm
compliance window is seeded at trial entry.

When \code{seed} is supplied, it is applied with \code{\link[withr:with_seed]{withr::with_seed()}}, which
restores the caller's \code{.Random.seed} afterwards, so reproducible simulation
does not perturb the surrounding random-number stream.
}
\examples{
sim <- simulate_screening_cohort(n = 200, seed = 1)
nrow(sim$cohort)
head(sim$screening_mammograms)

cloned <- clone_censor(
  sim$cohort, sim$screening_mammograms, sim$diagnostic_mammograms
)
nrow(cloned)
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=clone_censor]{clone_censor()}} for the next step in the pipeline, and \link{cohort},
\link{screening_mammograms}, \link{diagnostic_mammograms} for the shipped example
datasets this function reproduces.
}
