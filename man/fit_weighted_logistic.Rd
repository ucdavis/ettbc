% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_weighted_logistic.R
\name{fit_weighted_logistic}
\alias{fit_weighted_logistic}
\title{Fit a Weighted Pooled Logistic Regression}
\usage{
fit_weighted_logistic(data, formula, weight_col = NULL)
}
\arguments{
\item{data}{A data frame containing the model variables and, when
\code{weight_col} is supplied, the weight column.}

\item{formula}{A model formula passed to \code{\link[stats:glm]{stats::glm()}}.}

\item{weight_col}{Name of a numeric weight column in \code{data}, or \code{NULL}
(the default) for an unweighted fit.}
}
\value{
The fitted \link[stats:glm]{stats::glm} object (binomial family, logit link).
}
\description{
Fits a binomial (logit) generalized linear model from a formula and data,
optionally weighted by a named column. This is the shared fitting primitive
behind the package's IP-weighted models (\code{\link[=fit_outcome_hr]{fit_outcome_hr()}} and
\code{\link[=predict_survival_ipw]{predict_survival_ipw()}}): the weighted-GLM step now has a single, tested
implementation. It is exported so sibling packages can reuse the same
weighted pooled-logistic fit (see the discrete-time outcome-model
consolidation discussion in the package's issue tracker).
}
\examples{
df <- data.frame(
  y = c(0, 1, 0, 1, 1),
  x = c(1, 2, 3, 4, 5),
  w = c(1, 1, 2, 2, 1)
)
fit <- fit_weighted_logistic(df, y ~ x, weight_col = "w")
coef(fit)
}
\seealso{
\code{\link[=fit_outcome_hr]{fit_outcome_hr()}} and \code{\link[=predict_survival_ipw]{predict_survival_ipw()}}, which build their
formula and call this function for the fit.
}
