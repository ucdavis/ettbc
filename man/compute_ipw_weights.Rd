% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_ipw_weights.R
\name{compute_ipw_weights}
\alias{compute_ipw_weights}
\title{Compute Inverse Probability Weighting (IPW) Weights}
\usage{
compute_ipw_weights(
  long_data,
  pred_prob_col,
  arm_col = "arm",
  id_col = "id",
  month2_col = "month2",
  bc_month_col = "monthBC",
  scrmammo_col = "scrmammo",
  tslm_lag_col = "tslm_lag",
  grace_months = 11L
)
}
\arguments{
\item{long_data}{A data frame in long format (one row per
participant-arm-month), as produced by \code{\link[=expand_to_long]{expand_to_long()}} and augmented
with mammogram and time-since-last-mammogram columns.}

\item{pred_prob_col}{Name of the column containing the model-predicted
probability of a screening mammogram at each row.}

\item{arm_col}{Name of the trial arm column. Default: \code{"arm"}.}

\item{id_col}{Name of the participant identifier column. Default: \code{"id"}.}

\item{month2_col}{Name of the 0-indexed month-from-entry column.
Default: \code{"month2"}.}

\item{bc_month_col}{Name of the column containing the month2 at which breast
cancer was diagnosed (\code{NA} if no diagnosis). Default: \code{"monthBC"}.}

\item{scrmammo_col}{Name of the binary screening-mammogram indicator column.
Default: \code{"scrmammo"}.}

\item{tslm_lag_col}{Name of the lagged time-since-last-mammogram column
(months since the last any mammogram, screening or diagnostic).
Default: \code{"tslm_lag"}.}

\item{grace_months}{Number of months from trial entry during which weights
are held at 1 for the STOPBASE arm. Default: \code{11L}.}
}
\value{
\code{long_data} with two additional columns:
\itemize{
\item \code{w}: Cumulative IPW weight at each participant-arm-month.
\item \code{wp99}: IPW weight truncated at the 99th percentile of \code{w} within each
arm separately.
}
}
\description{
Computes stabilized inverse probability weights for each
participant-arm-month row in long-format data, based on the predicted
probability of receiving a screening mammogram. Weights are truncated at
the 99th percentile computed separately within each arm.
}
\details{
Separate weight models are used for the two trial arms:
\itemize{
\item \strong{STOPBASE}: The weight tracks the probability of \emph{not} receiving a
screening mammogram. After a grace period of \code{grace_months} months, the
denominator is \code{1 - p_scrmammo} at each month (or 1 if a breast cancer
diagnosis has occurred). If the time since last mammogram is 10 or fewer
months (\code{tslm_lag <= 10}), the effective screening probability is set to
0 (no screening expected that soon).
\item \strong{CONTINUE}: The compliance window is reset by any mammogram (screening
or diagnostic), as measured by \code{tslm_lag}. A weight update occurs at
every month within the compliance window (\code{tslm_lag} = 11, 12, or 13).
The numerator uses the conditional probability of the observed action
under a discrete uniform distribution over months 11–13: 1/3 at month 11,
1/2 at month 12 (given no earlier screening), and 1 at month 13. The
denominator is the model-predicted probability. Weights stop updating
after a breast-cancer diagnosis.
}

Weights within each arm are cumulative products initialized at 1.
The final column \code{wp99} is the weight truncated at the 99th percentile of
\code{w} computed \strong{separately within each arm}.
}
\examples{
cloned <- clone_censor(cohort, screening_mammograms, diagnostic_mammograms)
long_data <- expand_to_long(cloned)
long_data$p_scrmammo <- 0.3
long_data$monthBC <- NA_integer_
long_data$scrmammo <- 0L
long_data$anymammo <- 0L
long_data$tslm_lag <- 5L
result <- compute_ipw_weights(long_data, pred_prob_col = "p_scrmammo")
head(result[, c("id", "arm", "month2", "w", "wp99")])
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381–389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=predict_survival_ipw]{predict_survival_ipw()}}, \code{\link[=expand_to_long]{expand_to_long()}}
}
