% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/probabilistic_bias_analysis.R
\name{probabilistic_bias_analysis}
\alias{probabilistic_bias_analysis}
\title{Probabilistic Bias Analysis for an Unmeasured Confounder}
\usage{
probabilistic_bias_analysis(
  rd,
  prev_continue,
  prev_stopbase,
  confounder_effect,
  rd_se = 0,
  n_sim = 1000L,
  conf_level = 0.95,
  seed = NULL
)
}
\arguments{
\item{rd}{Observed arm risk difference (CONTINUE minus STOPBASE).}

\item{prev_continue, prev_stopbase, confounder_effect}{Either a single value
(fixed) or a length-2 \code{c(min, max)} uniform range, as described above.}

\item{rd_se}{Standard error of the observed risk difference. Default: \code{0}
(ignore sampling uncertainty, propagate only the bias parameters).}

\item{n_sim}{Number of Monte Carlo iterations. Default: \code{1000L}.}

\item{conf_level}{Width of the reported simulation interval. Default: \code{0.95}.}

\item{seed}{Optional integer seed. The caller's RNG state is restored on
exit. Default: \code{NULL}.}
}
\value{
A named list with \code{estimate} (median adjusted risk difference),
\code{conf_low} and \code{conf_high} (the simulation-interval bounds), \code{draws} (the
\code{n_sim} adjusted risk differences), \code{n_sim}, and \code{conf_level}.
}
\description{
Monte Carlo version of \code{\link[=deterministic_bias_analysis]{deterministic_bias_analysis()}}: instead of a single
set of assumptions, the bias parameters are drawn repeatedly from prior
distributions and combined with the sampling uncertainty in the observed risk
difference, yielding a simulation interval for the bias-adjusted effect. This
ports the probabilistic unmeasured-confounding sensitivity analysis of
García-Albéniz et al. (supplementary analysis).
}
\details{
On each of \code{n_sim} iterations the function draws the observed risk difference
from a normal distribution (\code{rd}, \code{rd_se}), draws each bias parameter, and
computes the bias-adjusted risk difference
\code{rd_draw - (prev_continue - prev_stopbase) * confounder_effect}. The reported
interval is the central \code{conf_level} percentile range of those draws.

Each bias parameter (\code{prev_continue}, \code{prev_stopbase}, \code{confounder_effect})
may be given as a single number (held fixed) or as a length-2 vector
\code{c(min, max)} (drawn from a uniform distribution on that range). Set \code{rd_se}
to \code{0} to propagate only the bias-parameter uncertainty.
}
\examples{
probabilistic_bias_analysis(
  rd = -0.012,
  rd_se = 0.004,
  prev_continue = c(0.01, 0.10),
  prev_stopbase = 0.05,
  confounder_effect = c(0.005, 0.02),
  n_sim = 2000,
  seed = 1
)
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=deterministic_bias_analysis]{deterministic_bias_analysis()}} for the single-assumption version.
}
