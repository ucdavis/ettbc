% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/compute_rcs_basis.R
\name{compute_rcs_basis}
\alias{compute_rcs_basis}
\title{Compute Restricted Cubic Spline (RCS) Basis}
\usage{
compute_rcs_basis(x, rcs_knots)
}
\arguments{
\item{x}{Numeric vector of values (e.g., follow-up time).}

\item{rcs_knots}{Numeric vector with at least 3 elements: first and last are
boundary knots; all intermediate elements are interior knots.}
}
\value{
A matrix with \code{length(rcs_knots) - 2} columns named \code{rcs1}, \code{rcs2},
..., one per nonlinear restricted-cubic-spline degree of freedom.
Restricted cubic splines are constrained to be linear beyond the boundary
knots.
}
\description{
Computes the \emph{nonlinear} restricted-cubic-spline basis terms for a numeric
vector, following Harrell's truncated-power parameterization as implemented
by the SAS \verb{\%RCSPLINE} macro. With \code{k} knots this returns \code{k - 2} nonlinear
terms (named \code{rcs1}, \code{rcs2}, ...); the linear term is supplied separately
(e.g., the raw time variable in a model formula). Together the linear term
and these nonlinear terms form a full-rank basis for the restricted cubic
spline (i.e. natural cubic spline) space, avoiding the rank deficiency that
arises when a separate linear term is added to a basis (such as
\code{\link[splines:ns]{splines::ns()}}) that already spans the linear component.
}
\details{
Each term is exactly zero for \code{x} at or below the first knot. In the
\code{{ettbc}} outcome models this means every spline term vanishes at month 0,
so the \code{STOPBASE} main effect in \code{\link[=fit_outcome_hr]{fit_outcome_hr()}} is the arm contrast at
baseline.

The basis is used internally by \code{\link[=fit_screening_propensity]{fit_screening_propensity()}} and the
\verb{predict_survival_*()} functions, and is exported so other packages can
reuse the same Harrell parameterization rather than writing it again.

For knots \verb{t[1] < ... < t[k]}, term \code{j} (for \verb{j = 1, ..., k - 2}) is
\verb{((x - t[j])_+^3 - (x - t[k-1])_+^3 * (t[k] - t[j]) / (t[k] - t[k-1])}
\verb{+ (x - t[k])_+^3 * (t[k-1] - t[j]) / (t[k] - t[k-1])) / (t[k] - t[1])^2},
where \verb{(u)_+ = max(u, 0)}. The \code{n_knots >= 3} guard ensures at least one
nonlinear term is produced.
}
\examples{
basis <- compute_rcs_basis(0:12, rcs_knots = c(0, 6, 12))
head(basis)
# Terms vanish at or below the first knot
basis[1, ]
}
\references{
Harrell FE. \emph{Regression Modeling Strategies.} Springer; 2015.
}
\seealso{
\code{\link[=fit_outcome_hr]{fit_outcome_hr()}} and \code{\link[=predict_survival_ipw]{predict_survival_ipw()}}, which model time
with this basis.
}
