% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/deterministic_bias_analysis.R
\name{deterministic_bias_analysis}
\alias{deterministic_bias_analysis}
\title{Deterministic Bias Analysis for an Unmeasured Confounder}
\usage{
deterministic_bias_analysis(
  rd,
  prev_continue,
  prev_stopbase,
  confounder_effect
)
}
\arguments{
\item{rd}{Observed arm risk difference (CONTINUE minus STOPBASE).}

\item{prev_continue}{Confounder prevalence in the CONTINUE arm (0-1).}

\item{prev_stopbase}{Confounder prevalence in the STOPBASE arm (0-1).}

\item{confounder_effect}{Additive effect of the confounder on outcome risk
(risk when present minus risk when absent), on the same scale as \code{rd}.}
}
\value{
A data frame with one row per assumption set, containing
\code{rd}, \code{prev_continue}, \code{prev_stopbase}, \code{confounder_effect}, \code{bias}
(\code{(prev_continue - prev_stopbase) * confounder_effect}), and \code{adjusted_rd}
(\code{rd - bias}).
}
\description{
Adjusts an observed arm risk difference for a single hypothetical
dichotomous, time-fixed unmeasured confounder, given its prevalence in each
arm and its effect on the outcome. This ports the deterministic
unmeasured-confounding sensitivity analysis of García-Albéniz et al.
(supplementary analysis): how far would the screening effect move if a
confounder of a specified strength, more common in one arm than the other,
were left uncontrolled?
}
\details{
For a dichotomous confounder with prevalence \code{prev_continue} in the CONTINUE
arm and \code{prev_stopbase} in the STOPBASE arm, and an additive effect
\code{confounder_effect} on the outcome risk (the risk when the confounder is
present minus the risk when it is absent), the bias it induces in the arm
risk difference (CONTINUE minus STOPBASE) is
\code{(prev_continue - prev_stopbase) * confounder_effect}. The bias-adjusted risk
difference is \code{rd} minus that bias.

The arguments are recycled to a common length, so passing vectors sweeps a
grid of assumptions in one call (e.g., several confounder prevalences against
a fixed effect). Express \code{rd} and \code{confounder_effect} on the same scale (a
risk difference, e.g., cases per person or per 1000); the result is on that
scale.
}
\examples{
# A confounder 4 points more common in STOPBASE, raising outcome risk by 0.01
deterministic_bias_analysis(
  rd = -0.012,
  prev_continue = c(0.01, 0.05, 0.10),
  prev_stopbase = 0.05,
  confounder_effect = 0.01
)
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=probabilistic_bias_analysis]{probabilistic_bias_analysis()}} for the Monte Carlo version.
}
