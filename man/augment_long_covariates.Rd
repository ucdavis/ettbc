% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/augment_long_covariates.R
\name{augment_long_covariates}
\alias{augment_long_covariates}
\title{Augment Long-Format Data with Mammogram and Time-Since-Last-Mammogram Columns}
\usage{
augment_long_covariates(
  long_data,
  screening_mammograms,
  diagnostic_mammograms,
  id_col = "id",
  arm_col = "arm",
  month_col = "month",
  month2_col = "month2",
  bc_long_col = "bc_long",
  event_month_col = "month",
  dx_reclass_months = 8L
)
}
\arguments{
\item{long_data}{A data frame in long format (one row per
participant-arm-month), as produced by \code{\link[=expand_to_long]{expand_to_long()}}. Must contain the
\code{id}, \code{arm}, \code{month}, \code{month2}, and \code{bc_long} columns (or as specified via
the \verb{*_col} arguments).}

\item{screening_mammograms}{A data frame of screening mammogram events with
participant ID and calendar month columns.}

\item{diagnostic_mammograms}{A data frame of diagnostic mammogram events with
participant ID and calendar month columns.}

\item{id_col}{Name of the participant ID column. Default: \code{"id"}.}

\item{arm_col}{Name of the trial arm column. Default: \code{"arm"}.}

\item{month_col}{Name of the calendar-month column (same scale as the
mammogram event months). Default: \code{"month"}.}

\item{month2_col}{Name of the 0-indexed month-from-entry column.
Default: \code{"month2"}.}

\item{bc_long_col}{Name of the per-month breast-cancer-diagnosis indicator
column. Default: \code{"bc_long"}.}

\item{event_month_col}{Name of the calendar-month column in the mammogram
event data frames. Default: \code{"month"}.}

\item{dx_reclass_months}{A screening mammogram occurring this many months or
fewer after the previous mammogram is reclassified as diagnostic.
Default: \code{8L}.}
}
\value{
\code{long_data} with six additional columns:
\itemize{
\item \code{scrmammo}: Screening-mammogram indicator for the month (0/1).
\item \code{dxmammo}: Diagnostic-mammogram indicator for the month (0/1).
\item \code{anymammo}: Any-mammogram indicator for the month (0/1).
\item \code{tslm}: Months since the last mammogram at this month.
\item \code{tslm_lag}: \code{tslm} from the previous month (\code{NA} at entry).
\item \code{monthBC}: \code{month2} of breast-cancer diagnosis (\code{NA} if none).
}
}
\description{
Adds the time-varying screening covariates required by
\code{\link[=compute_ipw_weights]{compute_ipw_weights()}} and \code{\link[=fit_screening_propensity]{fit_screening_propensity()}} to long-format
data produced by \code{\link[=expand_to_long]{expand_to_long()}}. It merges the observed mammogram
events onto each participant-arm-month and derives the screening indicator,
the time since the last mammogram, and the breast-cancer diagnosis month.
}
\details{
The construction follows the SAS \code{cann17b} long-covariate augmentation. For
each participant-arm, with rows ordered by \code{month2}:
\enumerate{
\item \code{scrmammo} and \code{dxmammo} are set to 1 in months with a screening or
diagnostic mammogram event, respectively, and 0 otherwise.
\item A screening mammogram is forced at trial entry (\code{month2 == 0}).
\item \code{anymammo} is 1 when either a screening or diagnostic mammogram occurred
that month. The time-since-last-mammogram clock is reset by any mammogram.
\item A screening mammogram in the month immediately after a breast-cancer
diagnosis is not counted as screening (\code{scrmammo} set to 0).
\item \code{tslm} is the running count of months since the last mammogram (0 in any
month with a mammogram, otherwise the previous value plus 1). \code{tslm_lag}
is \code{tslm} from the previous month (\code{NA} at entry), and is the value used
downstream.
\item A screening mammogram within \code{dx_reclass_months} months of the previous
mammogram (\code{tslm_lag <= dx_reclass_months}) is reclassified as diagnostic:
\code{dxmammo} set to 1 and \code{scrmammo} set to 0.
}

\code{monthBC} is the \code{month2} value at which breast cancer was diagnosed
(constant within each participant-arm, \code{NA} if no diagnosis), derived from
the \code{bc_long} indicator. It is the column \code{\link[=compute_ipw_weights]{compute_ipw_weights()}} expects via
its \code{bc_month_col} argument.
}
\examples{
cloned <- clone_censor(cohort, screening_mammograms, diagnostic_mammograms)
long_data <- expand_to_long(cloned)
long_data <- augment_long_covariates(
  long_data,
  screening_mammograms,
  diagnostic_mammograms
)
head(long_data[, c("id", "arm", "month2", "tslm_lag", "monthBC")])
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=expand_to_long]{expand_to_long()}} for the preceding step,
\code{\link[=fit_screening_propensity]{fit_screening_propensity()}} and \code{\link[=compute_ipw_weights]{compute_ipw_weights()}} for the steps that
consume these columns.
}
