% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expand_to_long.R
\name{expand_to_long}
\alias{expand_to_long}
\title{Expand Cloned Data to Long Format}
\usage{
expand_to_long(
  data,
  id_col = "id",
  arm_col = "arm",
  start_col = "start_month",
  end_col = "end_month",
  died_col = "died",
  bc_died_col = "bc_died",
  bc_month_col = "bc_month"
)
}
\arguments{
\item{data}{Output from \code{\link[=clone_censor]{clone_censor()}}, or any data frame with the same
structure. Must contain columns \code{id}, \code{arm}, \code{start_month}, \code{end_month},
\code{died}, \code{bc_died}, and \code{bc_month} (or as specified via \verb{*_col} arguments).}

\item{id_col}{Name of the participant ID column. Default: \code{"id"}.}

\item{arm_col}{Name of the trial arm column. Default: \code{"arm"}.}

\item{start_col}{Name of the start month column. Default: \code{"start_month"}.}

\item{end_col}{Name of the end month column. Default: \code{"end_month"}.}

\item{died_col}{Name of the overall death indicator column.
Default: \code{"died"}.}

\item{bc_died_col}{Name of the breast cancer death indicator column.
Default: \code{"bc_died"}.}

\item{bc_month_col}{Name of the breast cancer diagnosis month column.
Default: \code{"bc_month"}.}
}
\value{
A data frame with one row per participant-arm-month, containing:
\itemize{
\item \code{id}: Participant identifier
\item \code{arm}: Trial arm (\code{"STOPBASE"} or \code{"CONTINUE"})
\item \code{month}: Calendar month (integer, same scale as input)
\item \code{month2}: Month relative to trial entry, 0-indexed
\item \code{dead_t1}: Death in the next interval: 1 / 0 / \code{NA}
\item \code{bc_dead_t1}: Breast cancer death in the next interval
\item \code{bc_long}: Breast cancer diagnosis at this month (0/1)
}
}
\description{
Converts the output of \code{\link[=clone_censor]{clone_censor()}} from one row per participant-arm to
one row per participant-arm-month. The resulting dataset is in a format
suitable for discrete-time survival analysis with inverse probability
weighting (IPW).
}
\details{
The binary outcome variable \code{dead_t1} encodes whether the participant died
in the interval from month \code{t} to month \code{t+1}:
\itemize{
\item \code{0}: alive at \code{t+1} (confirmed)
\item \code{1}: died by \code{t+1}
\item \code{NA}: censored at month \code{t} (follow-up ends, outcome unknown)
}

For participants who died:
\itemize{
\item Rows span from \code{start_month} to \code{end_month - 1}.
\item The last row (at \code{end_month - 1}) has \code{dead_t1 = 1}.
\item No row is created for \code{end_month}.
}

For censored or surviving participants:
\itemize{
\item Rows span from \code{start_month} to \code{end_month}.
\item The last row (at \code{end_month}) has \code{dead_t1 = NA}.
\item All prior rows have \code{dead_t1 = 0}.
}

If a participant died in the same month they entered the study
(\code{end_month == start_month} and \code{died == 1}), \code{dead_t1} is set to \code{NA}
(outcome indeterminate within a single month).

The breast-cancer-specific outcome \code{bc_dead_t1} mirrors \code{dead_t1} but is
set to \code{0} at the event row when the death was not attributed to breast
cancer (\code{bc_died == 0}).
}
\examples{
cloned <- clone_censor(
  cohort,
  screening_mammograms,
  diagnostic_mammograms
)
long_data <- expand_to_long(cloned)
head(long_data)
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381–389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=clone_censor]{clone_censor()}} for the preceding step.
}
