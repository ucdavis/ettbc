% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_screening_propensity.R
\name{fit_screening_propensity}
\alias{fit_screening_propensity}
\title{Fit the Screening-Propensity Model}
\usage{
fit_screening_propensity(
  long_data,
  covariates = character(0),
  scrmammo_col = "scrmammo",
  tslm_lag_col = "tslm_lag",
  month2_col = "month2",
  id_col = "id",
  rcs_knots = c(13, 16, 25, 27),
  min_tslm_lag = 11L,
  pred_col = "p_scrmammo"
)
}
\arguments{
\item{long_data}{A data frame in long format augmented by
\code{\link[=augment_long_covariates]{augment_long_covariates()}}. Must contain the \code{scrmammo}, \code{tslm_lag}, and
\code{month2} columns (or as specified via the \verb{*_col} arguments), plus every
column named in \code{covariates}.}

\item{covariates}{Character vector of additional covariate column names to
include in the model. Default: none. Supply the baseline and time-varying
adjustment covariates here when analyzing real cohort data.}

\item{scrmammo_col}{Name of the binary screening-mammogram outcome column.
Default: \code{"scrmammo"}.}

\item{tslm_lag_col}{Name of the lagged time-since-last-mammogram column.
Default: \code{"tslm_lag"}.}

\item{month2_col}{Name of the 0-indexed month-from-entry column.
Default: \code{"month2"}.}

\item{id_col}{Name of the participant ID column. Default: \code{"id"}.}

\item{rcs_knots}{Numeric vector of restricted-cubic-spline knots for
\code{tslm_lag}. Default: \code{c(13, 16, 25, 27)} (the SAS \code{tslm_lagII} knots).}

\item{min_tslm_lag}{Minimum \code{tslm_lag} for a row to enter the model fit and
receive a prediction. Default: \code{11L}.}

\item{pred_col}{Name of the predicted-probability column to add to the
returned data. Default: \code{"p_scrmammo"}.}
}
\value{
A list with two elements:
\itemize{
\item \code{model}: The fitted \code{glm} object (binomial family, logit link).
\item \code{data}: \code{long_data} with the \code{pred_col} column added (\code{NA} outside the
\code{tslm_lag >= min_tslm_lag} decision window).
}
}
\description{
Fits a pooled logistic regression for the probability of receiving a
screening mammogram at each eligible participant-month, and returns the
fitted model together with the predicted probabilities merged onto the
input data. These predictions are the \code{pred_prob_col} consumed by
\code{\link[=compute_ipw_weights]{compute_ipw_weights()}}.
}
\details{
This ports the SAS \code{cann17b} denominator (\verb{\%cann17b_all_model}) propensity
model. The outcome is the screening-mammogram indicator \code{scrmammo}. The
linear predictor combines:
\itemize{
\item the time since the last mammogram, \code{tslm_lag}, as both a linear term and a
restricted-cubic-spline basis (knots at \code{rcs_knots}, using the same
Harrell parameterization as \code{\link[=predict_survival_ipw]{predict_survival_ipw()}});
\item month from entry as \code{month2} and \code{month2^2};
\item any additional baseline or time-varying covariates named in \code{covariates}.
}

The model is fit only on the decision window, the rows with
\code{tslm_lag >= min_tslm_lag}, matching the SAS \verb{where tslm_lag >= 11}. Because
the propensity model is arm-independent (the SAS model is fit on the
uncloned person-time), the fitting sample is deduplicated to one row per
participant-month before fitting, so a participant is not counted twice for
appearing in both arms.

Predicted probabilities are returned for every row in the decision window;
rows outside it (including trial entry, where \code{tslm_lag} is \code{NA}) receive
\code{NA}. \code{\link[=compute_ipw_weights]{compute_ipw_weights()}} treats those rows as having no predicted
screening, consistent with the SAS weights program.
}
\examples{
cloned <- clone_censor(cohort, screening_mammograms, diagnostic_mammograms)
long_data <- expand_to_long(cloned)
long_data <- augment_long_covariates(
  long_data,
  screening_mammograms,
  diagnostic_mammograms
)
fit <- fit_screening_propensity(long_data)
weighted <- compute_ipw_weights(fit$data, pred_prob_col = "p_scrmammo")
head(weighted[, c("id", "arm", "month2", "w", "wp99")])
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381-389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=augment_long_covariates]{augment_long_covariates()}} for the preceding step and
\code{\link[=compute_ipw_weights]{compute_ipw_weights()}} for the step that consumes \code{pred_col}.
}
