% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/extract_mammograms.R
\name{extract_screening_mammograms}
\alias{extract_screening_mammograms}
\title{Extract Screening Mammograms from Medicare Claims}
\usage{
extract_screening_mammograms(
  claims,
  id_col = "bene_id",
  date_col = "thru_dt",
  hcpcs_col = "hcpcs_cd",
  hcpcs_codes = c("77057", "76092", "G0202", "G0203", "G0205"),
  ref_date,
  study_start_month = 1L
)
}
\arguments{
\item{claims}{A data frame of Medicare claims. Must contain columns
\code{id_col}, \code{date_col}, and \code{hcpcs_col}.}

\item{id_col}{Name of the beneficiary identifier column.
Default: \code{"bene_id"}.}

\item{date_col}{Name of the service date column (coercible to \code{Date}).
Default: \code{"thru_dt"}.}

\item{hcpcs_col}{Name of the HCPCS procedure code column.
Default: \code{"hcpcs_cd"}.}

\item{hcpcs_codes}{Character vector of HCPCS codes identifying screening
mammography claims. Default:
\code{c("77057", "76092", "G0202", "G0203", "G0205")}.}

\item{ref_date}{A \code{Date} object giving the reference date from which study
months are counted (e.g., the first day of the study period).}

\item{study_start_month}{Integer giving the month index assigned to
\code{ref_date}. Default: \code{1L}.}
}
\value{
A data frame with one row per matching claim, containing:
\itemize{
\item \code{id}: Beneficiary identifier (from \code{id_col}).
\item \code{month}: Study month index (integer, 1-indexed from \code{ref_date}).
}
}
\description{
Filters a Medicare carrier or outpatient claims file to rows containing
screening mammography HCPCS codes, and converts claim dates to a 1-indexed
study month relative to a reference date.
}
\details{
This is a \strong{template function} that cannot be run without access to
Medicare claims data. It is provided as a reference implementation for
users who have access to such data.

The default HCPCS codes for screening mammography are:
\itemize{
\item \code{"77057"}: Bilateral screening mammography (film)
\item \code{"76092"}: Bilateral screening mammography (superseded)
\item \code{"G0202"}: Bilateral screening mammography (digital)
\item \code{"G0203"}: Bilateral screening mammography (film, FQHC)
\item \code{"G0205"}: Bilateral screening mammography (digital, FQHC)
}
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381–389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=extract_diagnostic_mammograms]{extract_diagnostic_mammograms()}}, \code{\link[=extract_any_mammograms]{extract_any_mammograms()}},
\code{\link[=clone_censor]{clone_censor()}}
}
