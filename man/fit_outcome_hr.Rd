% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/fit_outcome_model.R
\name{fit_outcome_hr}
\alias{fit_outcome_hr}
\title{Fit IPW-Weighted Outcome Hazard Ratio Model}
\usage{
fit_outcome_hr(
  long_data,
  covariate_cols = NULL,
  weight_col = "wp99",
  outcome_col = "dead_t1",
  arm_col = "arm",
  month_col = "month2",
  id_col = "id",
  cluster_id_col = id_col,
  max_month = 95L,
  rcs_knots = c(6, 48, 72)
)
}
\arguments{
\item{long_data}{A data frame in long format (one row per
participant-arm-month), as produced by \code{\link[=expand_to_long]{expand_to_long()}}.}

\item{covariate_cols}{Character vector of column names to include as
additional baseline adjustment terms. Set to \code{NULL} for no adjustment.
Default: \code{NULL}.}

\item{weight_col}{Name of the column containing IPW weights. Set to \code{NULL}
for unweighted estimation. Default: \code{"wp99"}.}

\item{outcome_col}{Name of the binary outcome column. Default: \code{"dead_t1"}.}

\item{arm_col}{Name of the trial arm column. Default: \code{"arm"}.}

\item{month_col}{Name of the 0-indexed month-from-entry column.
Default: \code{"month2"}.}

\item{id_col}{Name of the participant identifier column. Default: \code{"id"}.}

\item{cluster_id_col}{Name of the column to use for clustering standard
errors. When non-\code{NULL} and the \code{sandwich} package is available,
cluster-robust confidence intervals are returned. Defaults to \code{id_col}.}

\item{max_month}{Maximum month included in the model. Rows beyond this
month are excluded. Default: \code{95L}.}

\item{rcs_knots}{Numeric vector of length 3: \code{c(left_boundary, interior_knot, right_boundary)} for the restricted cubic spline on time.
Default: \code{c(6, 48, 72)}.}
}
\value{
A named list with three elements:
\itemize{
\item \code{model}: The fitted \link[stats:glm]{stats::glm} object.
\item \code{or}: Odds ratio for the STOPBASE arm (exp of the STOPBASE coefficient).
\item \code{or_ci}: Named numeric vector of length 2 giving the 95\% confidence
interval for the odds ratio (cluster-robust if \code{sandwich} is available
and \code{cluster_id_col} is set, otherwise Wald).
}
}
\description{
Fits an inverse-probability-weighted pooled logistic regression to estimate
the odds ratio (as a hazard ratio approximation) for the STOPBASE arm
relative to the CONTINUE arm, with time modeled via restricted cubic
splines.
}
\details{
The model formula is:

\if{html}{\out{<div class="sourceCode">}}\preformatted{dead_t1 ~ STOPBASE + STOPBASE:month + STOPBASE:rcs1 + STOPBASE:rcs2
          + month + rcs1 + rcs2 [+ covariate_cols]
}\if{html}{\out{</div>}}

Participant-level IPW weights from \code{weight_col} are passed to
\code{\link[stats:glm]{stats::glm()}}. The returned odds ratio corresponds to the \code{STOPBASE}
coefficient, which approximates the hazard ratio when the event is rare.

Because the dataset contains repeated person-month observations per
participant, standard \code{\link[stats:glm]{stats::glm()}} confidence intervals understate
uncertainty. When \code{cluster_id_col} is provided and the \code{sandwich} package
is installed, cluster-robust (HC) variance estimates are used to form the
confidence interval, matching the variance estimation in the SAS
\verb{PROC SURVEYLOGISTIC} implementation.
}
\examples{
cloned <- clone_censor(cohort, screening_mammograms, diagnostic_mammograms)
long_data <- expand_to_long(cloned)
long_data$wp99 <- 1.0
result <- fit_outcome_hr( # nolint: line_length_linter
  long_data, covariate_cols = NULL, weight_col = "wp99"
)
result$or
}
\references{
García-Albéniz X, Uno H, Bhatt DL, McArdle PH, Joffe MM, Hernán MA.
Continuation of Annual Screening Mammography and Breast Cancer Mortality in
Women Older Than 70 Years: A Prospective Observational Study.
\emph{Ann Intern Med.} 2020;172(6):381–389. \doi{10.7326/M18-1199}
}
\seealso{
\code{\link[=compute_ipw_weights]{compute_ipw_weights()}}, \code{\link[=predict_survival_ipw]{predict_survival_ipw()}},
\code{\link[=expand_to_long]{expand_to_long()}}
}
