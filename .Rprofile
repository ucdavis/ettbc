message("starting project-specific .Rprofile")
Sys.setlocale(category = "LC_COLLATE", locale = "en_US.UTF-8")

if (interactive()) {
  suppressMessages(require(devtools))
}

message("ending project-specific .Rprofile")
